[package]
name = "lambda"
version = "0.1.0"
edition = "2024"


[dependencies]
lazy_static = "1.5.0"

[profile.dev]
opt-level = 3

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
strip = true
panic = "abort"
overflow-checks = false